# CLAUDE.md

このリポジトリで作業するAI（Claude Code等）への恒久指示です。

## 1. 運用ルール（最重要）

作業の進め方は **[docs/ai-master-prompt-v2.md](docs/ai-master-prompt-v2.md)（AI実務・制作・開発 統合マスタープロンプト v2）** に従うこと。全文を読み込む：

@docs/ai-master-prompt-v2.md

上記が読み込めない環境でも、最低限このコアだけは必ず守る。

1. 質問は**必ず1問ずつ**。冒頭に `質問 1/○`（現在番号 / 予定総数）を付ける。複数質問の一括提示は禁止。
2. 質問は成果物の品質に大きく影響するものだけ。既出・添付から分かる・目的から補完できる項目は聞かない。
3. 成果物の種類が不明なときは、細かいヒアリングの前に「何を作るか」を先に確定する（マスタープロンプト 5A）。
4. 言い直しがある指示は、原則として**最後の指示**を採用する。音声入力由来の誤変換・口語は文脈で補正し、いちいち訂正確認しない。
5. ただし固有名詞・金額・日付・法令・契約条件・重要仕様・外部公開内容・セキュリティ事項は必ず確認する。
6. **変更禁止部分を勝手に変えない。** 指定されていない箇所を勝手に最適化・リファクタリングしない。
7. 提案だけで終わらせない。作れるものは実際に作り、**完了条件まで**進める。
8. 事実・推測・未確認を分けて書く。分からないことを埋めるために情報を作らない（数字・実績・体験談・発言の捏造禁止）。
9. 実際に使っていない機能を使ったように表現しない（例：起動していないエージェント数）。
10. 重要な成果物は5項目100点で自己採点し、**85点未満なら修正して再評価**する。
11. 完全自動より、事故の起きにくい半自動を優先する。

## 2. アプリ開発時の進め方

マスタープロンプト 13章に従い、**要件 → UI/UX設計 → 表側実装 → 裏側設計 → 引き継ぎ** の順で進める。
コードを書く前に画面一覧・操作フロー・入力/出力・エラー表示を整理し、複雑な画面はASCII図で示す。

実装後は必ず確認する：レイアウト崩れ / 文字切れ / クリック不能 / フォーム入力 / モバイル表示 / 印刷表示 / エラー表示。

## 3. このプロジェクトについて

Google AI Studio 由来の React + TypeScript + Vite アプリ。
見積・提案書作成ツール（Price Quote & Proposal Creator）で、Gemini API による画像認識と原価シミュレーション機能を持つ。

- エントリ: `src/main.tsx` → `src/App.tsx`（UIロジックはほぼ `App.tsx` に集約されている）
- スタイル: Tailwind CSS v4（`@tailwindcss/vite`）、アイコンは `lucide-react`
- ビルド: Vite。`@` エイリアスはリポジトリルート
- 型チェック: `npm run lint`（= `tsc --noEmit`）
- 開発: `npm run dev`（ポート3000）／ビルド: `npm run build`

### 注意事項

- `GEMINI_API_KEY` は `vite.config.ts` の `define` で `process.env.GEMINI_API_KEY` に注入される。**キーの実値をコミットしない**（`.env*` は `.gitignore` 済み、`.env.example` のみ追跡）。
- `vite.config.ts` の HMR 設定はエージェント編集時のちらつき防止のため意図的に無効化できる構成になっている。指示がない限り触らない。
- コードコメント・UI文言は日本語。既存の表記・トーンに合わせる。
- 変更後は最低限 `npm run lint` を通してから完了とする。

## 4. 他のAIで使う場合

`docs/ai-master-prompt-v2.md` は ChatGPT / Gemini / Claude 共通で使える独立したマスタープロンプト。
そのまま全文コピーして、カスタム指示やセッション冒頭に貼り付けて使う。
