rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // 10-5: 本人専用アプリのため、自分のuid配下のみ read/write を許可する。
    match /users/{uid}/{document=**} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
